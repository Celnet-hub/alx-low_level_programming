#include "main.h"

/**
 * add: function that adds two numbers
 *@a: is the first parameter passed to the function
 *@b: is the second parameter passed to the function
 *Return: the sum of the two parameters
 */

int add(int a, int b)
{
return (a + b);
}
